import {
  search,
  searchAlbums,
  searchArtists,
  searchPlaylists,
  searchTracks,
} from './search';

import {
  getAlbum,
  getAlbums,
  getAlbumTracks,
} from './albums';

import {
  getArtist,
  getTopTracksFromArtist,
} from './artists';

module.exports = {
  search,
  searchAlbums,
  searchArtists,
  searchPlaylists,
  searchTracks,
  getAlbum,
  getAlbums,
  getAlbumTracks,
  getArtist,
  getTopTracksFromArtist,
};
